# âš ï¸ These files will NEVER be uploaded to GitHub

# Secret environment variables â€” keep safe!
.env

# Node modules â€” Railway installs these automatically
node_modules

# System files
.DS_Store
Thumbs.db
